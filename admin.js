const REPO='KFB-LEAGUE/KFB-League.github.io',PATH='player-stats.js';let token='',sha='',players={};const TEAMS=['Yakult Swallows','KT wiz','Hyundai Unicorns','Samsung Lions'];const FIELDS=['gp','pa','ab','h','doubles','triples','hr','rbi','bb','so','stolen_bases','wins','era'];const $=id=>document.getElementById(id);function decode(b){return decodeURIComponent(escape(atob(b)))}function encode(s){return btoa(unescape(encodeURIComponent(s)))}function parse(text){const out={};const re=/"([^"]+)"\s*:\s*\{([^}]*)\}/g;let m;while((m=re.exec(text))){const p={};const fr=/([A-Za-z_][\w]*)\s*:\s*(?:"([^"]*)"|(-?\d+(?:\.\d+)?))/g;let f;while((f=fr.exec(m[2])))p[f[1]]=f[2]!==undefined?f[2]:Number(f[3]);out[m[1]]=p}return out}function makeFile(){let s='/* KFB League - MANUAL PLAYER STATISTICS\n * Managed through the Admin panel. AVG/OBP/SLG are calculated by the website.\n */\nconst playerStats = {\n';Object.keys(players).sort((a,b)=>a.localeCompare(b)).forEach((n,i)=>{const p=players[n];const vals=[`team:${JSON.stringify(p.team||'')}`,...FIELDS.map(k=>`${k}:${Number(p[k]||0)}`)];s+=`  ${JSON.stringify(n)}: { ${vals.join(', ')} }${i<Object.keys(players).length-1?',':''}\n`});return s+'};\n'}function render(){const q=($('search').value||'').toLowerCase(),t=$('team').value;const list=Object.entries(players).filter(([n,p])=>(!q||n.toLowerCase().includes(q))&&(!t||p.team===t));$('players').innerHTML=list.map(([n,p])=>`<article class="admin-player"><div class="admin-player-head"><strong>${n}</strong><button data-delete="${encodeURIComponent(n)}">삭제</button></div><select class="admin-select" data-key="team" data-name="${encodeURIComponent(n)}"><option value="">팀 선택</option>${TEAMS.map(x=>`<option value="${x}" ${x===p.team?'selected':''}>${x}</option>`).join('')}</select><div class="admin-fields">${FIELDS.map(k=>`<label>${k}<input data-key="${k}" data-name="${encodeURIComponent(n)}" type="number" step="any" value="${p[k]??0}"></label>`).join('')}</div></article>`).join('')||'<div class="admin-box">검색 결과가 없습니다.</div>';$('players').querySelectorAll('[data-key]').forEach(el=>el.oninput=()=>{const n=decodeURIComponent(el.dataset.name);players[n][el.dataset.key]=el.dataset.key==='team'?el.value:Number(el.value)});$('players').querySelectorAll('[data-delete]').forEach(el=>el.onclick=()=>{const n=decodeURIComponent(el.dataset.delete);if(confirm(`${n} 선수를 삭제할까요?`)){delete players[n];render()}})}async function load(){token=$('token').value.trim();if(!token){$('login-status').textContent='GitHub token을 입력하세요.';return}try{const r=await fetch(`https://api.github.com/repos/${REPO}/contents/${PATH}`,{headers:{Authorization:`Bearer ${token}`,Accept:'application/vnd.github+json'}});if(!r.ok)throw new Error('GitHub 인증/권한을 확인하세요.');const d=await r.json();sha=d.sha;players=parse(decode(d.content));$('login').classList.add('hidden');$('workspace').classList.remove('hidden');render();$('status').textContent=`${Object.keys(players).length}명 불러옴.`}catch(e){$('login-status').textContent=e.message}}async function save(){if(!token||!sha)return;$('status').textContent='GitHub에 저장하는 중...';try{const r=await fetch(`https://api.github.com/repos/${REPO}/contents/${PATH}`,{method:'PUT',headers:{Authorization:`Bearer ${token}`,Accept:'application/vnd.github+json','Content-Type':'application/json'},body:JSON.stringify({message:'Update player statistics from admin panel',content:encode(makeFile()),sha})});if(!r.ok)throw new Error('저장 실패. Fine-grained token의 Contents: Read and write 권한을 확인하세요.');const d=await r.json();sha=d.content.sha;$('status').textContent='저장 완료! 선수검색은 잠시 후 새 데이터로 표시됩니다.'}catch(e){$('status').textContent=e.message}}$('load').onclick=load;$('save').onclick=save;$('search').oninput=render;$('team').onchange=render;$('add').onclick=()=>{let n=prompt('새 선수 닉네임');if(!n||players[n])return;players[n]={team:'',gp:0,pa:0,ab:0,h:0,doubles:0,triples:0,hr:0,rbi:0,bb:0,so:0,stolen_bases:0,wins:0,era:0};render()};